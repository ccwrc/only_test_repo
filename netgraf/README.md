Napisać funkcję z wykorzystaniem języka PHP, która zwróci tablicę
zawierającą pełne ścieżki kategorii. W przypadku wywołania:


     echo $t[6];  // ID 6 - klucz


Otrzymamy wynik: "TELEFON >> INNE >> MODEL P"
